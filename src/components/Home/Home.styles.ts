import { base, Theme } from '../../styles';

export default (theme: Theme) => {
    return {
        ...base(theme),
        root: {
            columnCount: 3,
            columnGap: 15
        },
        name: {

        },
        weather: {

        },
        temp: {

        }

    };
};